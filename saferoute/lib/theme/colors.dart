import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const primaryOrange = Color(0xFFFF9800); // AppBar
  static const alertRed = Color(0xFFE53935); // Late alert
  static const routeOrange = Color(0xFFFF5722); // Route deviation
  static const successGreen = Color(0xFF4CAF50); // Drop-off success
  static const etaTeal = Color(0xFF009688); // ETA icon

  // Text & surface
  static const darkText = Color(0xFF212121);
  static const lightBackground = Color(0xFFF5F5F5);
  static const white = Colors.white;
  static const black = Colors.black;

  // Custom Map Marker color
  static const busMarkerBlue = Color(0xFF4285F4);
}
